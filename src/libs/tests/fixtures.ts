import { entitiesFixtures } from '../../entities/fixtures';
import { dbFixtures } from '../../repositories/database/fixtures';

export const fixtures = {
  entities: entitiesFixtures,
  db: dbFixtures,
};
